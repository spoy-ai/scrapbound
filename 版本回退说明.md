# 稳定版与回退

当前固定存档：**v0.3.0**，包含无尽模式、三条构筑路线、场中事件、主动暴走及全部运行美术、动画、音乐。

## 最简单的后悔药

双击 **恢复稳定版.command**。它会在游戏旁边新建 `scrapbound-v0.3.0-restored-随机字符`，恢复存档并打开游戏。正在修改的目录、未提交文件和后续版本都会保留；它不执行强制重置，也不删除文件。

恢复来源优先使用本地 Git 标签；原仓库不可用时，使用旁边 `scrapbound-backups/v0.3.0/` 的离线 Git bundle。如果在其他电脑使用，把 GitHub 发布里的 bundle 放到游戏目录旁边即可。首次启动会自动让已安装的 Godot 导入资源。

## GitHub 存档

- 仓库：`spoy-ai/scrapbound`，初始为私有，访问需要登录有权限的账号。
- `main` 是本次交付的代码，`v0.3.0` 是固定的稳定标签。
- `scrapbound-v0.3.0-project.zip`：完整运行项目，解压后可以用 Godot 启动。
- `scrapbound-v0.3.0.git.bundle`：独立离线 Git 存档，包含代码、资源、提交和版本标签；无需 GitHub 也能恢复。
- `scrapbound-v0.3.0-art-sources.zip`：原始图片、视频、提示词和制作资料。排除了平台响应中的临时签名链接、请求记录、重复抽帧、缓存及临时日志。
- `SHA256SUMS.txt`：下载文件的校验值，可用 `shasum -a 256 -c SHA256SUMS.txt` 检查。

上述离线文件同时保存在原游戏目录旁边的 `scrapbound-backups/v0.3.0/`。项目 ZIP 不包含可重新生成的 `.godot` 缓存；素材 ZIP 解压到项目根目录后恢复 `source/` 文件夹。所有原始平台记录仍保留在原工作目录内。

本次备份针对游戏工程和资源。个人游玩纪录仍由 Godot 保存在本机用户目录，恢复工程不会把它回滚。

## 后续开发约定

保留 `v0.3.0`，不移动或删除这个标签。新一轮修改先建立开发分支，阶段完成后提交；验证通过再合入 `main`，并为新的可玩版本创建新标签和发布。不要用强制推送覆盖已有稳定记录。

原电脑不可用时，可以在新电脑登录 GitHub 克隆仓库，也可以从发布页下载项目 ZIP 或 bundle。运行仍需要 Godot 4 标准版，本次验证版本是 4.7.2；发布包不是自带引擎的独立安装包。
